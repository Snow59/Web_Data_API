# Utilisez une image Java comme base
FROM openjdk:21

# Définissez le répertoire de travail dans le conteneur
WORKDIR /app

# Copiez le JAR de votre application dans le conteneur
COPY target/demo-0.0.1-SNAPSHOT.jar /app/invocation.jar

# Commande d'exécution pour démarrer l'application
CMD ["java", "-jar", "invocation.jar"]
